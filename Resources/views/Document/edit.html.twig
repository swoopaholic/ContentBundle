{% extends '::base.html.twig' %}

{% block content -%}
    {{ parent() }}
    <div class="btn-group">
        <a class="btn btn-default" href="{{ path('swoopaholic_document_admin') }}">Back to the list</a>
    </div>
    <h1>Page edit</h1>

    {{ form(edit_form) }}

    {#<ul class="record_actions">#}
        {#<li>{{ form(delete_form) }}</li>#}
    {#</ul>#}
{% endblock %}
